import{json}from'@sveltejs/kit';import type{RequestHandler}from'./$types.js';import{decideProposal,evaluationSnapshot,generateProposals}from'$lib/jarvis/evaluation/runtime.js';
export const GET:RequestHandler=async()=>json(await evaluationSnapshot());export const POST:RequestHandler=async({request})=>{try{const body=await request.json();if(body.operation==='generate')return json({proposals:await generateProposals()});if(body.operation==='decide')return json({proposal:await decideProposal(body.id,body.status)});throw new Error('Unknown operation')}catch(error){return json({error:error instanceof Error?error.message:String(error)},{status:400})}};
